/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package javaapplication43;

/**
 *
 * @author vaio
 */
public class JavaApplication43 {

  
    
    public static void main(String[] args) {
        int turtle = 10 * (2 + (3 + 2) / 5);
        int hare = turtle < 5 ? 10 : 25;
        System.out.println(turtle < hare ? "Hare wins!" : "Turtle wins!");
        /*
        Turtle is assigned to 30 because of the operation orders: 
        a.	(3+2) / 5
        b.	2+1
        c.	10*3 = 30
        Hare is assigned to 25 because turtle is not less than hare so the output is “Turtle wins!”
        */
        
    }
    
}
